module.exports = {
  up: async (queryInterface: any, Sequelize: any) => {
    // Nenhuma operação de migração necessária
  },

  down: async (queryInterface: any, Sequelize: any) => {
    // Nenhuma operação de reversão necessária
  }
};
